---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: paperless-media-pv
  namespace: home
spec:
  storageClassName: paperless-media
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: "${NFS_SERVER}"
    path: "/volume1/paperless/media"
  mountOptions:
    - nfsvers=4.1
    - tcp
    - intr
    - hard
    - noatime
    - nodiratime
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paperless-media-pvc
  namespace: home
spec:
  storageClassName: papaerless-media
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi
